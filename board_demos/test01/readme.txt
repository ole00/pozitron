This demo does nothing visible. It is just a verification
your build environment is sane. If you manage to compile it
without errors and get a .hex file then you are all set up.

To compile enter the 'default' directory then type:
make

To run the compiled binary type:
sudo make burn
