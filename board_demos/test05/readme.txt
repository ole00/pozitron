This demo sets up a timer that triggers an interrupt,
then the interrupt toggles a bit on a peripheral without using interrupt routine (ISR).
Also a DMA channel is used to start transferring data from
memory to GPIO port pins a certain intervals defined by another timer.


